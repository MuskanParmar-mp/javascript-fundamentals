function Area(radius){
  console.log(3.14 * radius * radius);
}

let r= parseInt(prompt("enter radius"));




























// let r = prompt("Enter radius:");
// r = parseFloat(r);

// if (!isNaN(r) && r > 0) {
//   showArea(r); // Call function with radius
// } else {
//   alert("Please enter a valid number.");
// }
